Before handing off work: run `cargo test`; if releasing also run `cargo build --release`. Ensure `migrations/` stays in sync with DB schema and commit any changes. Verify systemd unit files (`systemd/webhook-auto-update.*`) plus `.env` template, and confirm state directory contents (`data/`, `last_payload.bin`, locks) look sane. Keep SQLite backups up to date after schema changes.